# إعداد مستشار نور الإسلام والموقع

## مستشار ChatGPT

تطبيق الهاتف لا يحتوي على مفتاح OpenAI داخله حتى لا يتم كشفه لأي شخص. لذلك يحتاج المستشار إلى خادم HTTPS عام يشغّل `server.ts`.

على الخادم أضف:

```env
OPENAI_API_KEY=ضع_المفتاح_في_أسرار_الخادم
OPENAI_MODEL=gpt-4o-mini
APP_URL=https://رابط-الخادم
```

وعند بناء تطبيق الهاتف أضف متغير البناء:

```env
VITE_API_BASE_URL=https://رابط-الخادم
```

في GitHub Actions أضف Repository secret باسم `VITE_API_BASE_URL`. لا تضع `OPENAI_API_KEY` في ملفات التطبيق أو في أي متغير يبدأ بـ `VITE_`.

إذا لم يتم وضع `VITE_API_BASE_URL` سيظهر تنبيه داخل المستشار لأن النسخة لا تعرف عنوان الخادم، وهذا مقصود لحماية مفتاح ChatGPT.

## الموقع ومواقيت الصلاة

عند أول تشغيل يطلب التطبيق بالترتيب:

1. السماح بالإشعارات.
2. السماح بالموقع.

يستخدم التطبيق إحداثيات GPS الفعلية لحساب مواقيت الصلاة عبر خدمة الحساب، وليس اسم المدينة فقط. إذا رفض المستخدم الموقع، يمكن اختيار الدولة والمدينة يدويًا من الإعدادات.

على Android يجب السماح بالموقع الدقيق، وعلى iPhone يجب السماح بـ **Precise Location**. لا تُرسل الإحداثيات إلى خادم ChatGPT؛ تُستخدم لحساب المواقيت واتجاه القبلة فقط.
